class WebRtcService {
  const WebRtcService();

  Future<String> startPreview() async {
    throw UnimplementedError(
      'WebRTC preview now requires a real backend-backed RTC setup flow.',
    );
  }
}
